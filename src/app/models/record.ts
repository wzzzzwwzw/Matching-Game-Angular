export class Record {
  constructor(
    public username: string,
    public punctuation: number,
    public cards: number,
    public disposedTime: number,
    public recordDate: number,
  ) {
  }
}
